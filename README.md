# HyanHub 个人主页项目

一个基于 Vue 3 的现代化个人主页项目，具有科技感设计和主题切换功能。

## ✨ 特性

- 🎨 **科技感设计** - 蓝色主题配色，带有渐变和动画效果
- 🌓 **主题切换** - 支持深色/浅色模式切换，并持久化保存
- 📱 **响应式设计** - 完美适配手机、平板和桌面设备
- ⚡ **性能优化** - 使用 Vite 构建，加载速度快
- 🎭 **动画效果** - 流畅的过渡和交互动画
- 📦 **模块化结构** - 清晰的文件组织，易于维护和扩展

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:5173 查看项目

### 生产构建

```bash
npm run build
```

### 代码检查

```bash
npm run lint
```

### 代码格式化

```bash
npm run format
```

## 📁 项目结构

```
src/
├── components/
│   ├── layout/              # 布局组件
│   │   ├── AppHeader.vue   # 顶部导航栏
│   │   └── AppFooter.vue   # 页脚
│   ├── sections/            # 页面区块
│   │   ├── HeroSection.vue        # 首屏
│   │   ├── AboutSection.vue       # 关于我
│   │   ├── ProjectsSection.vue    # 项目展示
│   │   └── ContactSection.vue     # 联系方式
│   └── common/              # 通用组件
│       └── ThemeToggle.vue        # 主题切换按钮
├── assets/
│   ├── styles/              # 样式文件
│   │   ├── variables.css   # CSS 变量（主题色）
│   │   ├── global.css      # 全局样式
│   │   └── animations.css  # 动画效果
│   └── data/                # 数据文件
│       └── projects.js     # 项目和个人信息数据
├── composables/             # 组合式函数
│   └── useTheme.js         # 主题管理
├── router/                  # 路由配置
├── App.vue                  # 根组件
└── main.js                  # 入口文件
```

## 🎨 自定义配置

### 修改个人信息

编辑 `src/assets/data/projects.js` 文件：

```javascript
export const personalInfo = {
  name: '你的名字',
  title: '你的职位',
  bio: '个人简介',
  avatar: '头像链接',
  location: '所在地',
  email: 'your.email@example.com',
  // ... 其他信息
}
```

### 添加项目

在 `src/assets/data/projects.js` 中的 `projects` 数组添加新项目：

```javascript
export const projects = [
  {
    id: 1,
    title: '项目名称',
    description: '项目描述',
    tags: ['Vue3', 'Vite'],
    image: '项目图片链接',
    link: '项目链接',
    github: 'GitHub 仓库链接',
    featured: true, // 是否为特色项目
  },
  // 添加更多项目...
]
```

### 修改主题颜色

编辑 `src/assets/styles/variables.css` 文件中的 CSS 变量：

```css
:root {
  --color-primary: #0066ff; /* 主色调 */
  --color-accent: #00d9ff;  /* 强调色 */
  /* 更多颜色变量... */
}
```

### 修改技能列表

在 `src/assets/data/projects.js` 中修改 `skills` 数组：

```javascript
export const skills = [
  {
    category: '技能分类',
    items: ['技能1', '技能2', '技能3'],
  },
  // 添加更多技能分类...
]
```

## 🛠️ 技术栈

- **框架**: Vue 3.5.22
- **构建工具**: Vite 7.1.11
- **路由**: Vue Router 4.6.3
- **代码规范**: ESLint + Prettier
- **开发工具**: Vue DevTools

## 📝 开发说明

### 添加新的区块组件

1. 在 `src/components/sections/` 创建新组件
2. 在 `src/App.vue` 中导入并使用
3. 添加对应的导航链接到 `AppHeader.vue`

### 修改动画效果

所有动画都定义在 `src/assets/styles/animations.css` 中，可以直接修改或添加新动画。

### 主题切换原理

主题切换使用 `data-theme` 属性控制，在 `src/composables/useTheme.js` 中管理状态，支持：
- 自动检测系统主题偏好
- 手动切换主题
- localStorage 持久化存储

## 📱 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Edge
- 移动端浏览器

## 📄 许可证

MIT License

## 👨‍💻 作者

Hyan - [GitHub](https://github.com/yourusername)

---

如有问题或建议，欢迎提 Issue 或 PR！
